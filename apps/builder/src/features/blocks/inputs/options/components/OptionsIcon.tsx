import { CheckSquareIcon } from '@/components/icons'
import { IconProps } from '@chakra-ui/react'
import React from 'react'

export const OptionsInputIcon = (props: IconProps) => (
  <CheckSquareIcon color="orange.500" {...props} />
)
