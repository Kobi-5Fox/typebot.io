import { NumberIcon } from '@/components/icons'
import { IconProps } from '@chakra-ui/react'
import React from 'react'

export const PancardIcon = (props: IconProps) => (
  <NumberIcon color="orange.500" {...props} />
)
